-- kt_character/client/utils.lua
-- FIX : ce fichier était inutilisable (variables characters/slots non définies).
--       La logique de sélection de personnage est gérée dans client/main.lua
--       via les events union:spawn:selectCharacter et characters:openSelection.
--       Ce fichier est conservé vide pour ne pas casser le glob client/*.lua.

-- (intentionnellement vide — voir client/main.lua pour la gestion NUI)